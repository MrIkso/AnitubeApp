<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/carousel_main_card"
    style="?attr/materialCardViewFilledStyle"
    android:layout_width="match_parent"
    android:layout_height="@dimen/interesting_anime_card_height"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="@android:color/white"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Parallax Layer 1 (Back) -->
        <ImageView
            android:id="@+id/carousel_bg_1"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="matrix"
            android:alpha="0.8"
            app:tint="#EEEEEE"
            app:tintMode="multiply"
            android:contentDescription="@null" />

        <!-- Parallax Layer 2 (Front) - Mirrored -->
        <ImageView
            android:id="@+id/carousel_bg_2"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="matrix"
            android:alpha="0.7"
            app:tint="#E0E0E0"
            app:tintMode="multiply"
            android:scaleX="-1"
            android:contentDescription="@null" />

        <ImageView
            android:id="@+id/carousel_imageView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:scaleType="fitCenter"
            tools:srcCompat="@tools:sample/backgrounds/scenic" />

    </FrameLayout>

</com.google.android.material.card.MaterialCardView>
